namespace pkk {
    /**
     * Test pkk
     */
    //% blockId=pkkpoinkk
    //% block = "plot at index"
    //% a.min=0 a.max=4
    export function poinkk(a: number, b: number) : void {
        const x = a + b;
        led.plot(a,b);
    }
}
